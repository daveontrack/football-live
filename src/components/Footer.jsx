import { footerStyles as s } from "../assets/dummyStyles";

export default function Footer() {
  return (
    <footer className={s.footer}>
      <p className={s.wordmark}>ዳዊት football</p>
    </footer>
  );
}
